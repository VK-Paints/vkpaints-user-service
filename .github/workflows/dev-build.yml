name: Dev Build & Push
on:
  push:
    branches: [ main ]
jobs:
  build:
    uses: VK-Paints/vkpaints-workflow/.github/workflows/build-push-dev.yml@main
    with:
      service_name: user-service
    secrets: inherit
